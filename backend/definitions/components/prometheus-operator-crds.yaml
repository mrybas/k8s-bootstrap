# Prometheus Operator CRDs
# Hidden chart - auto-included when any monitoring component is selected

id: prometheus-operator-crds
name: Prometheus Operator CRDs
description: Custom Resource Definitions for Prometheus Operator (ServiceMonitor, PodMonitor, PrometheusRule, etc.)
category: crds
categoryName: CRDs
categoryDescription: Custom Resource Definitions
icon: "📋"
priority: 1  # CRDs must be installed first
docsUrl: https://prometheus-operator.dev/docs/prologue/introduction/

# Hidden from UI - auto-included when monitoring is selected
hidden: true
autoInclude:
  when:
    - kube-prometheus-stack
    - victoria-metrics-operator
    - prometheus

chartType: upstream
namespace: cluster-crds
releaseName: prometheus-operator-crds
createNamespace: false  # Created by repo_generator for all CRDs

upstream:
  repository: https://prometheus-community.github.io/helm-charts
  chartName: prometheus-operator-crds
  version: "26.0.0"

defaultValues: {}

# No UI schema - this is auto-managed
